语义化的概念：

指对文本内容的结构化（内容语义化），选择合乎语义的标签（代码语义化），
便于开发者阅读，维护和写出更优雅的代码的同时，让浏览器的爬虫和辅助技术更好的解析。



何为标签语义化？

根据标签的语义，在合适的地方放一个最合适的标签，可以让页面结构更清晰。
标签语义化有助于构架良好的html结构，有利于搜索引擎的建立索引、抓取，
有利于页面在不同的设备上显示尽可能相同，有利于构建清晰的结构，有利于团队的开发、维护。